<section class="orders-show">
  <header class="page-header">
    <h1>Order #<%= @order.id %></h1>
  </header>

  <div class="order-summary jumbotron">
    <h4>Thank you for your order!</h4>
  </div>

  <% @order.line_items.each do |line_item| %>
    <div class="line-item">
      <%= image_tag line_item.product.image.thumb.url, alt: line_item.product.name %>
      <div class="line-item-details">
        <h3><%= line_item.product.name %></h3>
        <p><%= line_item.product.description %></p>
        <p>Quantity: <%= line_item.quantity %></p>
        <p>Total: <%= humanized_money_with_symbol line_item.total_price %></p>
      </div>
    </div>
  <% end %>

  <div class="order-total">
    <h3>Order Total: <%= humanized_money_with_symbol @order.total_cents / 100.0 %></h3>
  </div>

  <div class="order-email">
    <p>Email: <%= @order.email %></p>
  </div>

  <%= link_to "Continue Shopping", root_path, class: 'btn btn-lg btn-primary' %>
</section>
